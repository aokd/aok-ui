---
order: 0
title: 
description:
---

```jsx
import { <%= title.replace(/^[\w]{1}/, ($1) => $1.toUpperCase()) %> } from 'aok'

ReactDOM.render(
  <React.Fragment>
  </React.Fragment>,
  mountNode
)
```
